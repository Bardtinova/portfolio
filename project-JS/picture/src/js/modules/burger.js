const burger = (menuSelector, burgerSelector) => {
    const menuElem = document.querySelector(menuSelector),
          bugerElem = document.querySelector(burgerSelector);
          
};

export default burger;